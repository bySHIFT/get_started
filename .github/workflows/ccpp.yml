name: THE3 CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: make
      run: make

  run:
    needs: build
    steps:
    - name: run
      run: |
        echo '放胆开怀 尽情肆意'
        ./the3
